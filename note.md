# 一、PCL相关
## 1.1 点云的定义
```cpp
// cloud1定义的是一个点云容器，存储了多个pcl::PointXYZRGBNormal类型的点
pcl::PointCloud<pcl::PointXYZRGBNormal> cloud1;
// cloud2定义的是一个单独的点，表示一个具体的点
pcl::PointXYZRGBNormal cloud2;
```
###  区别
`pcl::PointCloud` 是 PCL 中表示点云数据的容器类型，它是一个模板类，可以存储任意类型的点。
`pcl::PointCloud<pcl::PointXYZRGBNormal>` 表示一个点云，里面的每个点是 pcl::PointXYZRGBNormal 类型，
包含了点的位置（x, y, z），颜色（r, g, b），以及法线信息（normal_x, normal_y, normal_z）。
这意味着 `pcl::PointCloud<pcl::PointXYZRGBNormal>` 是一个点云数据结构，而 pcl::PointXYZRGBNormal 是点的类型。

# 二、第三方库配置方式
laslib库和CSF库的配置方式有何不同，两种方式分别是怎样的？
这是一个非常好的问题。通过对比这两种配置方式，你可以更清晰地理解 CMake 的工作原理。

### 1. 核心区别概述

| 特性 | LASlib (之前的方式) | CSF (现在的方式) |
| :--- | :--- | :--- |
| **库的来源** | 完整的第三方**工程** (Lastools) | 只有几个**源文件** (.cpp/.h) |
| **构建逻辑** | 调用它**自带**的 `CMakeLists.txt` | **自己写**一个 `CMakeLists.txt` 给它 |
| **包含路径** | 复杂 (因为是相对路径引用) | 简单 (直接指向当前目录) |
| **依赖关系** | LASlib 依赖 LASzip (嵌套依赖) | CSF 只依赖 OpenMP (系统库) |
| **适用场景** | 引用成熟的、结构复杂的第三方库 | 引用轻量的、甚至没有构建系统的源码 |

---

### 2. 方式一：LASlib 配置方式 (调用式)

**场景**：LAStools 是一个庞大的工程，它自己内部已经写好了复杂的 `CMakeLists.txt`，规定了如何编译 LASzip，如何编译 LASlib，以及它们之间的依赖关系。我们不想去改它的内部逻辑（虽然为了修复 bug 我们改了一点点路径），只想**“借用”**它的构建结果。

**流程**：
1.  `add_subdirectory(3rdparty/LAStools)`：告诉 CMake，“去那个目录下运行**它写好的**构建脚本”。
2.  LAStools 的脚本运行后，会产生 `LASlib` 和 `LASzip` 这两个 Target（目标）。
3.  我们在主程序中 `target_link_libraries(... LASlib)` 直接链接这个生成好的目标。

**特点**：
*   **省心**：不需要关心它内部有几百个源文件。
*   **被动**：配置选项受制于对方的 CMake 写法（比如之前的路径错误，就是因为它的 CMake 写得不规范）。

---

### 3. 方式二：CSF 配置方式 (托管式)

**场景**：CSF 只有几个 `.cpp` 和 `.h` 文件，可能还是从 GitHub 上直接抓下来的，连 `CMakeLists.txt` 都没有（或者写得很乱）。这时候，**我们自己充当它的构建管理者**。

**流程**：
1.  我们在 `3rdparty/CSF` 下**新建**了一个 `CMakeLists.txt`。
2.  我们在里面写 `file(GLOB ...)` 也就是告诉 CMake：“把这几个文件抓进来”。
3.  我们写 `add_library(CSF_Lib ...)`：告诉 CMake，“用我抓进来的这几个文件，编译成一个叫 `CSF_Lib` 的静态库”。
4.  主程序链接这个我们**亲手创建**的 `CSF_Lib`。

**特点**：
*   **完全掌控**：文件名叫什么、编成静态还是动态、开启什么编译选项（如 OpenMP），全由我们自己写。
*   **灵活**：非常适合这种“小而美”的算法库。
---
### 总结

*   **LASlib** 就像是你去餐馆**点菜**（调用它做好的菜单/CMakeLists）。
*   **CSF** 就像是你买了原材料（源码），在自家厨房**自己做菜**（自己写 CMakeLists 来编译它）。

如果你以后遇到其他库：
*   如果它有很规范的 `CMakeLists.txt`，用方式一。
*   如果它只有一堆源码，或者你想把它的代码直接融进你的项目里，用方式二。

# 三、进度条显示
在点云加载和其他一些操作中，我们通过处理的点数量来计算进度并显示
但是，对于PCL中内置函数，属于黑盒模式，无法获取准确进度，所以采用进度感知来显示进度，而非真正的进度

# 四、点云加载渲染优化方案
由于当前采用的框架是PCL+VTK， 如果像cloudcompare一样重写渲染引擎复杂且成本高，所以基于现有框架，分三步走优化方案
核心理念：LOD (Level of Detail) 动态降采样
核心痛点： 显卡和 CPU 无法在 16ms（60FPS）内把几千万个点全部画出来。
解决思路： 动时少画，静时全画。
当用户旋转、平移、缩放视图时（交互态），只渲染稀疏的点云（例如仅显示 100 万点）；当用户松开鼠标停止操作时（静态），再立刻渲染完整的点云。

## 4.1、交互式动态降采样
双数据备份，一份全量数据，一份降采样数据
1. 当用户旋转、平移、缩放视图时（交互态），只渲染稀疏的点云（例如仅显示 100 万点）
2. 当用户松开鼠标停止操作时（静态），再立刻渲染完整的点云。
加载优化

## 4.2、关闭渲染特效 (VTK 配置优化)
点云渲染不需要像 3D 模型那样复杂的光照和着色，这些特效在大数据量下是累赘。
关闭光照，关闭着色差值

## 4.3、大坐标偏移
这是导致“卡顿感”和“画面抖动”的隐形杀手。
问题：
激光雷达数据（如 LAS）通常是 UTM 坐标，坐标值巨大（例如 x = 500,000.00）。
GPU 在处理浮点数时，数字越大，精度越低（大数吃掉了小数部分的精度）,导致变化跨度大，也就是抖动现象
。这会导致画面在旋转时出现剧烈的锯齿和抖动，看起来像卡顿。

方案：
在加载点云时（FileIO 中），计算点云的重心 (Centroid)。
将所有点的坐标减去这个重心（Local Coordinates）。
记录这个重心偏移量（Global Shift）。
渲染时只渲染 Local Coordinates（此时坐标值很小，GPU 处理极快且精准）。
导出保存时，再把重心加回去。

# 五、软件闪退及卡顿优化
当前由于采用PCL+VTK框架的原因，由于PCL库很笨重，并且我们在分配内存时采用的是vector，如果内存不足，无法分配大片连续内存，就会导致软件闪退
其次，当前所采用的LOD策略也只能缓解渲染的卡顿，如果点云数据量过大，全部载入显存中，依旧会导致崩溃，软件闪退。
还有，缺乏索引信息，当找一个点或者进行裁剪时，需要遍历整个数组，效率低下

**优化思路**：
参考CloudCompare的方案，采用八叉树ccOctree
1、解决内存崩溃（分块管理）
八叉树将巨大的 3D 空间切分成无数个小立方体（节点）。
- 非连续内存：点云数据不再存储在一个巨大的 vector 里，而是分散存储在八叉树的叶子节点中。每个节点只存几万个点。
这样操作系统只需要分配无数个小的内存块，不需要找巨大的连续空间，极大降低了崩溃概率。
- 外存调度 (Out-of-Core)：进阶的八叉树（如 Potree）甚至不需要把所有点都读入 RAM。它可以只读入八叉树的索引结构，
当用户放大看某个区域时， 再动态从硬盘加载该区域的叶子节点数据。这能让你在 8G 内存的电脑上打开 100G 的点云。

2、解决渲染卡顿 (视锥剔除 & 动态 LOD)
视锥剔除 (Frustum Culling)：如果你把相机拉近只看兔子的耳朵，八叉树可以快速判断出“身体”、“脚”都不在相机视野内，直接丢弃，不送入 GPU 渲染。
多层级细节 (Hierarchical LOD)：
相机离得很远时：只渲染八叉树第 3 层的节点（每个节点只画一个代表点），一共画 1000 个点。
相机拉近一点：渲染第 5 层的节点。
贴在脸上看：渲染第 10 层的叶子节点（全精度）。
结果：无论你怎么缩放，屏幕上的像素是有限的，GPU 永远只需要画几十万个点，帧率永远锁定 60FPS。

**但是，基于当前框架，完全重构底层难度较大，暂时基于现有框架进行优化**

# 六、软件结构优化
## 当前问题：
1、耦合：底层逻辑（如 FileIO）本应只负责读写文件，现在却间接关联了 UI（虽然通过信号解耦了，但物理文件放在一起很乱）。
2、循环依赖风险：CloudTree（在 base）调用 GlobalShiftDialog（在 base），而 GlobalShiftDialog 又引用 Cloud（在 base）。
如果把 Dialog 移出去，CloudTree 留在 base，base 就会依赖上层 UI 库，而上层 UI 库又依赖 base 的数据类型，形成死锁。

## 新架构，采用三层架构
Level 1: pw_core (原 base 的提纯), ct_module
定位：pw_core:最底层数据与逻辑库。不依赖任何具体业务弹窗，只负责数据定义、算法接口和文件读写逻辑。
依赖：PCL, VTK, Eigen, Qt (Core, Gui, OpenGL)。
定位：ct_module:算法库，封装了算法接口，提供算法调用接口。
依赖：pw_core (需要使用 Cloud 类等数据结构)。

Level 2: ct_widget, ct_common_ui
定位：ct_common_ui:通用 UI 库。包含基础窗口类、通用对话框（如进度条、坐标偏移设置）。
依赖：pw_core (需要使用 Cloud 类等数据结构)。
定位：ct_widget:业务组件库，负责基础类与窗口类的交互。
依赖：pw_core ct_common_ui 

Level 3: cloudtool 
定位：高级业务组件库。将数据与 UI 结合的复杂控件。
依赖：pw_core (数据), ct_widget (弹窗)。

# 七、UI设计中的问题
- 如果空间之间无法对齐，尝试添加一个弹簧控件

# 八、git提交规范
一个标准的约定式提交格式通常如下：
```
<类型>(<作用域>): <描述>
```

| 前缀类型 | 英文全称       | 含义         | 对应场景举例 |
|----------|----------------|--------------|--------------|
| feat     | Feature        | 新功能       | 增加了一个按钮；添加了一个新的 API 接口 |
| fix      | Fix            | 修复 Bug     | 修复了点击按钮没反应的问题；修复了空指针报错 |
| docs     | Documentation  | 文档变动     | 仅修改了 README.md；修改了代码注释（不改逻辑） |
| style    | Style          | 代码格式     | 修改缩进、空格、分号；重命名变量（不影响代码运行逻辑） |
| refactor | Refactor       | 重构         | 代码重构，既不修 bug 也不加功能，只是优化代码结构 |
| perf     | Performance    | 性能优化     | 优化了算法，提升了页面加载速度 |
| test     | Test           | 测试         | 增加测试用例，或者修改现有的测试代码 |
| chore    | Chore          | 杂务 / 构建  | 修改构建流程（如 CMakeLists、package.json），更新依赖库 |
| revert   | Revert         | 回滚         | 撤销某次之前的提交 |

例如规范使用
```test
feat(ui): add export button
fix(core): fix null pointer crash
chore(build): update cmake version
```

